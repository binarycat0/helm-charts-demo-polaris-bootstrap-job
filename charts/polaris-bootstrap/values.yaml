bootstrap:
  credentials: "POLARIS,admin,changeme"
  realm: "POLARIS"

persistenceSecret:
  name: polaris-persistence
  usernameKey: username
  passwordKey: password
  jdbcUrlKey: jdbcUrl

image:
  repository: apache/polaris-admin-tool
  tag: latest
  pullPolicy: IfNotPresent

resources:
  {}